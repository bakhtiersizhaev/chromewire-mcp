{
  "name": "chromewire-mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "ChromeWire MCP connects the official Codex Chrome Extension to MCP-compatible AI CLIs for safe localhost control of real Chrome tabs.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/config.js && node --check src/redact.js && node --check src/server.js && node --check src/codexChromePipe.js && node --check scripts/smoke.js && node --check scripts/doctor.js && node --check scripts/install.js",
    "test": "node --test",
    "smoke": "node scripts/smoke.js",
    "doctor": "node scripts/doctor.js",
    "install:agent": "node scripts/install.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "classic-level": "^3.0.0",
    "express": "^5.1.0",
    "zod": "^4.1.12"
  },
  "license": "Apache-2.0",
  "author": "Original author (preserve attribution in NOTICE)",
  "homepage": "https://github.com/OWNER/codex-chrome-mcp-bridge#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OWNER/codex-chrome-mcp-bridge.git"
  },
  "bugs": {
    "url": "https://github.com/OWNER/codex-chrome-mcp-bridge/issues"
  },
  "bin": {
    "chromewire-mcp": "./src/server.js",
    "codex-chrome-mcp-bridge": "./src/server.js"
  },
  "files": [
    "src/",
    "scripts/",
    "docs/",
    "examples/",
    "skills/",
    ".github/workflows/ci.yml",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "keywords": [
    "ai-cli",
    "browser-automation",
    "chrome",
    "chromewire",
    "codex",
    "codex-chrome-extension",
    "mcp",
    "mcp-server",
    "real-chrome"
  ],
  "engines": {
    "node": ">=20"
  }
}
